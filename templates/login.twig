<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login - Ticket Management</title>
  <!-- Tailwind CDN -->
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="flex justify-center items-center min-h-screen bg-gray-100">

  <div class="flex flex-col justify-center px-4 items-center w-2/3 max-w-md h-auto rounded-lg bg-white bg-opacity-20 p-10 shadow-lg">
    <h1 class="text-2xl font-bold mb-6">Welcome Back</h1>

    {# ✅ Success message #}
    {% if success == 'account_created' %}
      <div class="mb-4 p-3 text-green-700 bg-green-100 rounded">
        Account created successfully! You can now log in.
      </div>
    {% endif %}

    {# ❌ Error messages #}
    {% if error == 'missing_fields' %}
      <div class="mb-4 p-3 text-red-700 bg-red-100 rounded">Please fill in all fields.</div>
    {% elseif error == 'user_not_found' %}
      <div class="mb-4 p-3 text-red-700 bg-red-100 rounded">No user found with that email.</div>
    {% elseif error == 'wrong_password' %}
      <div class="mb-4 p-3 text-red-700 bg-red-100 rounded">Incorrect password.</div>
    {% elseif error == 'no_users' %}
      <div class="mb-4 p-3 text-red-700 bg-red-100 rounded">No registered users yet. Please sign up first.</div>
    {% endif %}

    <!-- Login Form -->
    <form action="/login-handler.php" method="POST" class="flex flex-col w-full space-y-4">
      
      <label for="email" class="text-sm font-medium text-gray-700">Email</label>
      <input
        type="email"
        id="email"
        name="email"
        placeholder="example@gmail.com"
        required
        class="p-3 rounded border border-gray-300 focus:outline-none focus:ring-2 focus:ring-red-400"
      />

      <label for="password" class="text-sm font-medium text-gray-700">Password</label>
      <input
        type="password"
        id="password"
        name="password"
        placeholder="Password"
        required
        class="p-3 rounded border border-gray-300 focus:outline-none focus:ring-2 focus:ring-red-400"
      />

      <button
        type="submit"
        class="w-full bg-red-600 text-white font-semibold py-2 rounded hover:bg-blue-700 transition"
      >
        Login
      </button>
    </form>

    <!-- Signup Link -->
    <p class="mt-4 text-sm">
      Don’t have an account? 
      <a href="/signup.php" class="text-red-600 hover:underline">Sign Up</a>
    </p>
  </div>

</body>
</html>
